# Project Brief

AI Codebase Context Assistant is a practical developer tool for understanding a local codebase with AI-assisted retrieval.

The system will scan source files, preserve metadata and line references, store searchable chunks, retrieve relevant context for developer questions, and refuse to answer when the indexed context is insufficient.

The project should remain small, explainable, and easy to run locally.
